---
import { buildCitiesIndexModel } from '@generator';
import { index, siteUrl } from '../lib/data';
import HubPage from '../components/HubPage.astro';

const model = buildCitiesIndexModel(index, siteUrl);
---

<HubPage model={model} />
